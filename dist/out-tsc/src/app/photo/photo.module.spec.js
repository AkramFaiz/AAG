import { PhotoModule } from './photo.module';
describe('PhotoModule', function () {
    var photoModule;
    beforeEach(function () {
        photoModule = new PhotoModule();
    });
    it('should create an instance', function () {
        expect(photoModule).toBeTruthy();
    });
});
//# sourceMappingURL=photo.module.spec.js.map